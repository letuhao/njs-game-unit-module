/**
 * Size value enumeration
 * Defines the different types of size values
 */
export enum SizeValue {
  PIXEL = 'pixel',
  FILL = 'fill',
  AUTO = 'auto',
  FIXED = 'fixed',
  RESPONSIVE = 'responsive',
  RELATIVE = 'relative'
}
