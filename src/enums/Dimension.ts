/**
 * Dimension enumeration
 * Defines the different dimensions that can be affected by units
 */
export enum Dimension {
  WIDTH = 'width',
  HEIGHT = 'height',
  BOTH = 'both',
  X = 'x',
  Y = 'y',
  XY = 'xy',
  Z = 'z'
}
