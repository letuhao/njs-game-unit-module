/**
 * Scale value enumeration
 * Defines the different types of scale values
 */
export enum ScaleValue {
  FACTOR = 'factor',
  FIXED = 'fixed',
  RESPONSIVE = 'responsive',
  RELATIVE = 'relative',
  AUTO = 'auto',
  PIXEL = 'pixel',
  PERCENTAGE = 'percentage',
  VIEWPORT = 'viewport',
  PARENT = 'parent',
  FILL = 'fill',
  FIT = 'fit',
  COVER = 'cover',
  CONTAIN = 'contain',
  STRETCH = 'stretch',
  CENTER = 'center',
  TOP = 'top',
  BOTTOM = 'bottom',
  LEFT = 'left',
  RIGHT = 'right',
  RANDOM = 'random',
  CONTENT = 'content',
  MAINTAIN_ASPECT = 'maintain-aspect',
  IGNORE_ASPECT = 'ignore-aspect',
  CONTENT_SCALE = 'content-scale',
  INTRINSIC_SCALE = 'intrinsic-scale',
  BREAKPOINT_SCALE = 'breakpoint-scale',
  DEVICE_SCALE = 'device-scale'
}
