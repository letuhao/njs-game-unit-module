/**
 * Position value enumeration
 * Defines the different types of position values
 */
export enum PositionValue {
  FIXED = 'fixed',
  RESPONSIVE = 'responsive',
  RELATIVE = 'relative',
  CENTER = 'center',
  ALIGNED = 'aligned'
}
