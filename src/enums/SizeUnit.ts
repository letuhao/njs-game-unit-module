/**
 * Size unit enumeration
 * Defines the different types of size units
 */
export enum SizeUnit {
  PIXEL = 'pixel',
  PERCENT = 'percent',
  PARENT_WIDTH = 'parent-width',
  PARENT_HEIGHT = 'parent-height',
  VIEWPORT_WIDTH = 'viewport-width',
  VIEWPORT_HEIGHT = 'viewport-height',
  AUTO = 'auto'
}
