*** unit/classes/RandomValueNumber.ts — SOLID refactor template patches ***

@@ DIP-Construction (3 occurrences)
--- BEFORE ---
  L14: throw new Error('min cannot be greater than max');
  L30: throw new Error(`Value ${value} is outside the range [${this.min}, ${this.max}]`);
  L49: return new RandomValueNumber(
--- AFTER (template) ---
  // Replace direct constructions with DI resolve (composition root)
  // const svc = resolve(TOKENS.SizeCalc)

