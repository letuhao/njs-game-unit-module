*** unit/managers/StrategyManager.ts — SOLID refactor template patches ***

@@ DIP-Construction (2 occurrences)
--- BEFORE ---
  L36: private strategies: Map<string, IUnitStrategy> = new Map();
  L60: throw new Error(`Invalid strategy for unit type: ${strategy.unitType}`);
--- AFTER (template) ---
  // Replace direct constructions with DI resolve (composition root)
  // const svc = resolve(TOKENS.SizeCalc)


@@ SRP-Logging L3
--- ACTION ---
  Move logging/console from core to a LoggingDecorator. Remove logger imports.
