*** unit/examples/PhaserGameObjectExample.ts — SOLID refactor template patches ***

@@ DIP-Construction (2 occurrences)
--- BEFORE ---
  L123: this.container = new ResponsiveContainer(this, 400, 300);
  L128: const sprite = new ResponsiveSprite(this, 0, 0, 'sprite');
--- AFTER (template) ---
  // Replace direct constructions with DI resolve (composition root)
  // const svc = resolve(TOKENS.SizeCalc)

