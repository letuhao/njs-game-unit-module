*** unit/test/CalculatePositionCommand.test.ts — SOLID refactor template patches ***

@@ DIP-Construction (12 occurrences)
--- BEFORE ---
  L12: command = new CalculatePositionCommand(positionInput as any, mockContext);
  L50: const positionCommand = new CalculatePositionCommand(input as any, mockContext);
  L73: const invalidCommand = new CalculatePositionCommand(invalidInput as any, mockContext);
  L90: const numericCommand = new CalculatePositionCommand(numericInput as any, mockContext);
  L98: const arrayCommand = new CalculatePositionCommand(arrayInput as any, mockContext);
--- AFTER (template) ---
  // Replace direct constructions with DI resolve (composition root)
  // const svc = resolve(TOKENS.SizeCalc)

